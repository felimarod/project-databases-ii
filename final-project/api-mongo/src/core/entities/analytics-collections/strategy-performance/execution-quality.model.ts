export interface ExecutionQuality {
    avg_slippage: number; // Average slippage in pips
    avg_execution_time: number; // Average execution time in milliseconds
    fill_rate: number; // Percentage of orders filled
}